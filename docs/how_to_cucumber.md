Para começar tudo:

`bundle init`

Na gemfile:

gem 'capybara'
gem 'cucumber'
gem 'selenium-webdriver'
gem 'rspec'

No terminal:

`bundle install`

`bundle exec cucumber --init`

`touch cucumber.yml`

No cucumber.yml:

default: --publish-quiet

Pronto pra rodar...